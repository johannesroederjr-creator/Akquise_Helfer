#!/bin/sh
# docs/ vor jedem Commit mit akquise-helfer-website synchronisieren (GitHub Pages).
ROOT="$(git rev-parse --show-toplevel)"

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$ROOT/scripts/sync-docs.ps1" || exit 1
git add docs/

exit 0
